export type TzIndices = {
  modal: number;
  dropdown: number;
  tooltip: number;
  default: number;
};

export const zIndices: Record<string, number> = {
  modal: 100,
  dropdown: 50,
  tooltip: 25,
  default: 1,
};
